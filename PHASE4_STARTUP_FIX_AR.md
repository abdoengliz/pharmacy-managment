# المرحلة 4 — إصلاح تشغيل PostgreSQL

هذه النسخة تعالج أول خطأ حقيقي ظهر أثناء تشغيل التطبيق على Supabase:

`AUTOINCREMENT` داخل أوامر `CREATE TABLE` المنفذة عبر `db.execute()`.

## التعديل

أصبحت طبقة التوافق تحول تلقائيًا:

```sql
id INTEGER PRIMARY KEY AUTOINCREMENT
```

إلى PostgreSQL:

```sql
id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY
```

وذلك في مساري `execute()` و`executescript()`.

## التشغيل

شغّل الملف نفسه:

```powershell
powershell -ExecutionPolicy Bypass -File .\RUN_SUPABASE_APP_WINDOWS.ps1
```

إذا ظهر خطأ جديد، أرسل آخر جزء كامل من PowerShell. لا تحذف قاعدة Supabase ولا تعيد الترحيل.
