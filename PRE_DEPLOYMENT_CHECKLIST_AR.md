# قائمة التحقق قبل الرفع على الإنترنت

1. أنشئ قيمة طويلة وعشوائية للمتغير `SECRET_KEY` ولا تستخدم القيمة التجريبية.
2. شغّل الموقع خلف HTTPS، ثم اضبط `SESSION_COOKIE_SECURE=1`.
3. اضبط `HOST=0.0.0.0` فقط داخل بيئة الاستضافة، وليس على جهاز مكشوف بدون جدار حماية.
4. احتفظ بنسخة احتياطية من `pharmacy_finance.db` قبل أول تشغيل على الخادم.
5. لا تشغّل `FLASK_DEBUG=1` في الإنتاج.
6. قيّد الوصول إلى ملف قاعدة البيانات والنسخ الاحتياطية على مستخدم خدمة التطبيق فقط.
7. بعد الرفع اختبر: تسجيل الدخول، الموردين، الإيرادات، المخزون، إنشاء تحويل مخزني، الإرسال، والاستلام.

## الإصلاحات المؤكدة في v5.8.7.1

- جميع نقاط النهاية المستعملة في القوالب موجودة.
- مسارات المخزون وتحويلات المخزون أُعيدت بالكامل.
- إرسال واستلام التحويلات يعمل داخل معاملات قاعدة بيانات لمنع التحديث الجزئي.
- قاعدة البيانات المرفقة سليمة ولا تحتوي على علاقات أجنبية مكسورة.
