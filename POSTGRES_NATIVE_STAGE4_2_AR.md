# PostgreSQL Native Stage 4.2

تم إصلاح تهيئة الحساب النقدي الافتراضي لكل فرع بحيث تصبح العملية قابلة للتكرار بأمان على PostgreSQL.

الصيغة الجديدة تستخدم:

```sql
ON CONFLICT (branch_id, name) DO NOTHING
```

وبذلك لا يفشل تشغيل التطبيق إذا كان حساب الخزينة موجودًا مسبقًا.
