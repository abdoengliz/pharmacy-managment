# Pharma ERP v6.1 — Supabase Edition Phase 2

هذه المرحلة تنشئ جداول PostgreSQL في Supabase وتنقل البيانات الموجودة في `pharmacy_finance.db` مع التحقق من عدد السجلات.

## التشغيل على Windows

1. افتح PowerShell داخل المجلد.
2. ثبّت المتطلبات إن لم تكن مثبتة:

```powershell
python -m pip install -r requirements.txt
```

3. شغّل أداة الترحيل:

```powershell
powershell -ExecutionPolicy Bypass -File .\MIGRATE_TO_SUPABASE_WINDOWS.ps1
```

4. أدخل كلمة مرور قاعدة بيانات Supabase. لن تُحفظ الكلمة في ملفات المشروع.
5. بعد التحليل، اكتب `MIGRATE` بالإنجليزية لتأكيد بدء النقل.

## ما تفعله الأداة

- تنشئ نسخة احتياطية محلية قبل النقل.
- تحلل  قاعدة SQLite والجداول والسجلات.
- تحذف جداول ERP الموجودة في مشروع Supabase عند التأكيد فقط.
- تنشئ الجداول والمفاتيح الأساسية والفهارس والعلاقات.
- تنقل البيانات على دفعات.
- تضبط تسلسلات أرقام المعرفات.
- تقارن عدد السجلات بين SQLite وSupabase.
- تنشئ تقريرًا باسم `SUPABASE_MIGRATION_REPORT.json`.

## تنبيه

لا تشغّل التطبيق بوضع Supabase للإنتاج مباشرة بعد الترحيل. هذه المرحلة مخصصة لإنشاء القاعدة ونقل البيانات. بعدها يلزم اختبار تسجيل الدخول والمبيعات والمخزون والتقارير في نسخة تجريبية.
