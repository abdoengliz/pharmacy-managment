# المرحلة الثالثة: تشغيل تجريبي للتطبيق على Supabase

هذه نسخة Pilot لاختبار تشغيل الـERP نفسه على PostgreSQL بعد نجاح ترحيل الجداول والبيانات.

## التشغيل على Windows

```powershell
powershell -ExecutionPolicy Bypass -File .\RUN_SUPABASE_APP_WINDOWS.ps1
```

أدخل كلمة مرور قاعدة Supabase. عند نجاح الاتصال سيبدأ التطبيق على:

`http://127.0.0.1:5000`

## اختبار القبول الأولي

1. تسجيل الدخول.
2. فتح لوحة التحكم.
3. إضافة عميل تجريبي.
4. إضافة مورد أو إيراد تجريبي.
5. تسجيل الخروج والدخول مجددًا.
6. التأكد من ظهور السجل الجديد في Table Editor داخل Supabase.

لا تستخدم النسخة للإنتاج قبل نجاح الاختبارات الوظيفية الكاملة.
