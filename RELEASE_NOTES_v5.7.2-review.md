# Pharma ERP Enterprise v5.7.2 — Reviewed & Clean

هذه نسخة صيانة آمنة مبنية على v5.7.1، ولا تغيّر مخطط قاعدة البيانات أو منطق العمليات المالية.

## التعديلات المنفذة

- إزالة بيئة `venv` المضمنة من حزمة التوزيع؛ يتم إنشاؤها محليًا عند التثبيت.
- إزالة `__pycache__` و`.pytest_cache` وملفات Python المؤقتة.
- إزالة قاعدة البيانات الشاردة التي كانت داخل `__pycache__` تلقائيًا مع تنظيف المجلد المؤقت.
- إضافة `.gitignore` لمنع عودة الملفات المؤقتة والبيئة المحلية إلى الإصدارات القادمة.
- إيقاف وضع Debug افتراضيًا، وربطه بمتغير `FLASK_DEBUG` فقط.
- جعل عنوان الاستضافة والمنفذ قابلين للضبط عبر `HOST` و`PORT`، مع افتراضي آمن `127.0.0.1:5000`.
- استبدال المفتاح السري الثابت العام بمفتاح عشوائي محلي عند عدم توفير `SECRET_KEY`.
- دعم تحديد مسار قاعدة البيانات عبر `PHARMA_DB_PATH` مع الحفاظ على المسار القديم كافتراضي.
- إضافة ملف `.env.example` لتوضيح إعدادات التشغيل دون حفظ أسرار حقيقية.
- تحديث README ليتوافق مع الإصدار الحالي واتجاه النظام الإداري والمالي.

## التوافق

- قاعدة البيانات الرئيسية `pharmacy_finance.db` محفوظة دون تعديل.
- النسخة الاحتياطية الموجودة في مجلد `backups` محفوظة دون تعديل.
- لا توجد عمليات حذف لجداول أو أعمدة أو مسارات وظيفية.
- وحدة المبيعات القديمة بقيت مخفية مع بقاء الكود والمسارات للتوافق والرجوع الآمن.
