# تقرير تحسين الأداء — المرحلة الثانية (v6.3.6)

## ما تم تنفيذه في هذه الدفعة

- تحليل ثابت لجميع Routes داخل `app/routes_modules`.
- حصر استدعاءات قاعدة البيانات المباشرة داخل كل Route.
- اكتشاف الاستعلامات الموجودة داخل الحلقات والتي قد تسبب نمط N+1 أو رحلات شبكة متكررة.
- تحسين البحث العام `/search` بدمج البحث في:
  - الإيرادات
  - المصروفات
  - سدادات الموردين
  في استعلام PostgreSQL واحد باستخدام `UNION ALL` بدل ثلاثة استعلامات منفصلة.
- الحفاظ على نفس الصلاحيات، الفلاتر، وحدود النتائج وروابط الصفحات.

## نتيجة تحسين البحث العام

عندما يمتلك المستخدم صلاحيات الأقسام المالية الثلاثة:

- قبل: 3 رحلات منفصلة إلى Supabase للبحث المالي.
- بعد: رحلة واحدة إلى Supabase.
- التخفيض: رحلتان شبكيتان في كل عملية بحث عام.

هذا مهم على Vercel/Supabase لأن زمن الشبكة غالبًا أكبر من زمن تنفيذ الاستعلام نفسه.

## أعلى نقاط الأولوية التي كشفها التحليل

> الأرقام التالية هي عدد مواضع `execute` في الكود وليست قياس زمن حي على قاعدة الإنتاج.

| الأولوية | الصفحة / الدالة | الملف | استدعاءات DB المباشرة | الملاحظة |
|---|---|---|---:|---|
| 1 | Dashboard | `auth_dashboard.py` | 14 | ما زالت أكبر صفحة من حيث الرحلات المباشرة، رغم أنها محسّنة مقارنة بالنسخة القديمة |
| 2 | الموردون | `suppliers.py` | 10 | تحتاج دمج counters والاستعلامات المرجعية |
| 3 | تفاصيل المورد | `suppliers.py` | 10 | مرشحة لدمج المجاميع والحركات |
| 4 | إدارة النظام | `operations.py` | 10 | عدة counters مستقلة |
| 5 | تعديل تحويل خزينة | `finance.py` | 10 | أغلبها ضمن GET/POST ويجب فصل تحليل المسارين |
| 6 | تفاصيل الموظف | `hr.py` | 9 | عدة قوائم وتلخيصات في تحميل واحد |
| 7 | تعديل المستخدم | `users_access.py` | 9 | توجد عمليات داخل حلقات في مسار الحفظ |
| 8 | المدفوعات | `finance.py` | 8 | توجد استعلامات ضمن معالجة POST |
| 9 | الإيرادات | `finance.py` | 5 | استعلام داخل مسار POST مع استعلامات عرض |
| 10 | المخزون | `inventory.py` | 7 | يحتاج Pagination ومراجعة حجم النتائج |

## ملاحظات مهمة

- وجود استعلام داخل حلقة لا يعني دائمًا مشكلة في صفحة GET؛ بعض الحالات تقع في مسار POST لحفظ مجموعة عناصر. لذلك يجب تحسين كل حالة مع الحفاظ على المعاملة والنتيجة الوظيفية.
- الفهارس التي تم إنشاؤها في Supabase ستساعد الاستعلامات الحالية، لكن أكبر مكسب إضافي سيأتي من تقليل عدد رحلات الشبكة وإضافة Pagination.
- أعمدة التاريخ مخزنة كنصوص في المخطط الحالي؛ يجب الحفاظ على صيغة ISO موحدة حتى تعمل المقارنات والفهارس بكفاءة.

## التحقق

- نجح فحص الترجمة لكل ملفات `app` بواسطة `compileall`.
- تعذر تشغيل مجموعة `pytest` في بيئة التحضير لأن مكتبة Flask غير مثبتة في بيئة التنفيذ هنا، وليس بسبب خطأ تجميعي في التعديل.

## الدفعة التالية المقترحة

1. تحسين صفحة الموردين وتفاصيل المورد.
2. إضافة Pagination للموردين والمبيعات والمخزون والسجلات الكبيرة.
3. تقليل رحلات Dashboard الإضافية مع Cache قصير للإحصائيات غير الحساسة.
4. إضافة قياس اختياري لزمن الطلب وعدد الاستعلامات في بيئة الاختبار لتأكيد التحسن بالأرقام.
