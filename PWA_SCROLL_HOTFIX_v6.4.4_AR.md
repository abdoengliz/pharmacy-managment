# إصلاح تمرير الصفحات في تطبيق PWA — v6.4.4

- إعادة تفعيل التمرير الرأسي في وضع التطبيق المستقل على iOS وiPadOS وAndroid.
- إزالة تعارض `overscroll-behavior-y:none` مع بعض إصدارات Safari PWA.
- ضمان أن `html` و`body` و`.app-shell` لا تملك ارتفاعًا ثابتًا يمنع التمرير.
- تنظيف أصناف قفل الصفحة عند تشغيل التطبيق وعند الرجوع من الذاكرة الخلفية.
- إبقاء قفل الخلفية فقط أثناء فتح القائمة الجانبية أو لوحة الإجراءات فعلًا.
- الحفاظ على تمرير القوائم والجداول والنوافذ الداخلية باللمس.
- لا توجد تغييرات على قاعدة البيانات.
