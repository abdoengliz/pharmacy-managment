# إصدار تحسين الأداء v6.5.0

## التعديلات
- رفع كاش لوحة التحكم من 15 إلى 45 ثانية (قابل للتعديل عبر `DASHBOARD_CACHE_TTL`).
- كاش مرجعي لمدة 180 ثانية للفروع وإحصاءات الإدارة.
- إزالة استعلام `SELECT 1` الزائد من لوحة التحكم.
- تحويل ملفات PWA الثابتة إلى Cache-first مع تحديث بالخلفية.
- إضافة ترويسات Cache-Control للملفات الثابتة.
- إضافة `Server-Timing` لقياس زمن التطبيق وقاعدة البيانات وعدد الاستعلامات من أدوات المتصفح.
- لا تغيير على البيانات أو مخطط قاعدة البيانات أو التصميم.

## قياس السرعة
افتح DevTools > Network واختر أي طلب صفحة، ثم راقب ترويسة `Server-Timing`.
