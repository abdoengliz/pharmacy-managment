# Pharma ERP Enterprise v6.2.0 — PostgreSQL Native Stage 4.6

## الهدف
رفع اعتمادية المعاملات المالية ومنع بقاء اتصال PostgreSQL في حالة معاملة فاشلة بعد الاستثناءات.

## التعديلات
- إضافة Rollback تلقائي عند انتهاء طلب Flask باستثناء غير معالج، قبل إغلاق الاتصال.
- تطوير `audit()` لدعم `commit=False` حتى يمكن إدراج سجل التدقيق داخل نفس المعاملة المالية بدل تنفيذ Commit مستقل.
- تطبيق السجل الذري على العمليات الحرجة التالية:
  - إنشاء دين خارجي.
  - تعديل دين خارجي.
  - طلب اعتماد أو إرسال تحويل خزينة.
  - استلام تحويل خزينة ونقل الرصيد بين الحسابين.
- إضافة `app/transaction_utils.py` ودالة `atomic()` كأساس موحد للمعاملات القادمة.
- إضافة اختبارين لدالة المعاملة: Commit عند النجاح وRollback عند الفشل.

## النتيجة
أصبح سجل التدقيق في العمليات المحددة ينجح أو يتراجع مع العملية المالية نفسها، ولا يعود قادرًا على تثبيت جزء من العملية ثم ترك بقية البيانات غير مكتملة.
