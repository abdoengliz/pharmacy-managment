# PostgreSQL Native — Stage 4.4

بُنيت هذه المرحلة على Stage 4.3 المرجعية.

## التعديلات

- إزالة فحص `sqlite_master` من منطق تهيئة التطبيق.
- إضافة فحص محايد للمحرك للقيود الفريدة عبر `table_has_unique_constraint`.
- PostgreSQL يفحص القيود من `pg_catalog`، بينما دعم SQLite القديم معزول داخل `app/db_compat.py`.
- نقل آخر اعتماد على `cursor.lastrowid` من `app/core.py` إلى طبقة التوافق فقط.
- تدقيق كود التشغيل داخل `app/` مع استثناء طبقة التوافق: لا توجد أوامر SQLite تنفيذية من الأنماط المدققة.

## ملاحظة

طبقة `app/db_compat.py` ما زالت تحتوي دعم SQLite اختياريًا، وهذا مقصود للتشغيل المحلي القديم. مسارات الأعمال نفسها أصبحت معزولة عن هذه التفاصيل.
