# تحديث v6.5.4 — حذف سجل العمليات يدويًا

- إضافة زر **حذف السجل** داخل مركز التدقيق.
- الزر يظهر لمدير النظام فقط.
- تأكيد واضح قبل الحذف النهائي.
- حذف بيانات جدول `audit_log` فقط دون المساس بالمبيعات أو المخزون أو المستخدمين أو بقية البيانات.
- تحديث كاش لوحة التحكم بعد الحذف حتى تختفي العمليات القديمة مباشرة.
- حماية الطلب عبر POST وCSRF.
