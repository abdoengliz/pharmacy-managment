# المرحلة 7 — تحويل SQL شامل لـ PostgreSQL

- إصلاح استعلام إدارة المستخدمين بإزالة GROUP BY المتساهل الخاص بـ SQLite واستخدام استعلام فرعي لتجميع الصلاحيات.
- إصلاح صفحة الأدوار باستخدام عدّ مرتبط بدل GROUP BY على r.*.
- مراجعة جميع استعلامات GROUP BY وإضافة أعمدة الجداول المرتبطة المطلوبة في PostgreSQL.
- الإبقاء على طبقة التوافق لأوامر PRAGMA وsqlite_master وINSERT OR IGNORE وAUTOINCREMENT وتعديلات التاريخ.
- إضافة أداة تدقيق POSTGRES_COMPATIBILITY_AUDIT.py.

هذه نسخة اختبار توافق واسعة، وليست اعتماد إنتاج نهائي قبل اختبار كل وحدات النظام وظيفيًا.
