# PostgreSQL Native — Stage 2

## الإصلاح المنفذ

تم إصلاح إضافة `RETURNING id` التلقائية في طبقة التوافق.

في المرحلة السابقة كانت الطبقة تفترض أن كل جدول يحتوي على عمود `id`، لذلك فشل بدء التشغيل عند الإدخال في جدول `code_sequences` لأن مفتاحه ليس `id`.

النسخة الحالية:

- تستخرج اسم الجدول من جملة `INSERT`.
- تفحص `information_schema.columns` لمعرفة هل يوجد عمود `id` فعلاً.
- تضيف `RETURNING id` فقط للجداول التي تحتوي عليه.
- لا تضيف `RETURNING id` إلى `code_sequences` أو أي جدول بمفتاح مختلف.
- تخزن نتيجة الفحص مؤقتاً لتقليل الاستعلامات المتكررة.
- تمسح الذاكرة المؤقتة عند تنفيذ `CREATE/ALTER/DROP TABLE`.
- لا تستخدم `LASTVAL()`.

## التحقق

- نجح اختبار محاكاة إدخال في جدول بلا `id`.
- نجح اختبار محاكاة إدخال في جدول يحتوي على `id` واسترجاع `lastrowid`.
- نجح `python -m compileall` على المشروع.
