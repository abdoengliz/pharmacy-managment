# Pharma ERP Enterprise v5.4.0-dev

## Notification & Task Engine

- إنشاء `NotificationService` كخدمة مركزية للإشعارات.
- إنشاء `TaskService` كخدمة مركزية للمهام.
- دعم منع تكرار الإشعارات والمهام بواسطة `event_key`.
- دعم الروابط المباشرة للعملية بواسطة `action_url`.
- دعم تاريخ قراءة الإشعار وتاريخ استحقاق المهمة.
- دعم توجيه المهمة حسب المستخدم أو الفرع أو الصلاحية المطلوبة.
- ربط طلبات الاعتماد تلقائيًا بمهمة اعتماد وإشعار.
- إغلاق مهمة الاعتماد تلقائيًا عند اتخاذ القرار.
- إرسال نتيجة الاعتماد أو الرفض إلى مقدم الطلب.
- إضافة مؤشرات الإشعارات والمهام والاعتمادات إلى لوحة القيادة.
- ترقية قاعدة البيانات إلى الإصدار 5.4.

## التوافق

تم الحفاظ على دوال `create_notification` و`create_task` القديمة كواجهات متوافقة تستدعي المحرك الجديد، حتى لا تتأثر الوحدات الحالية.
