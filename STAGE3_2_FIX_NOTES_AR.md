# Stage 3.2 — إصلاح GREATEST داخل Python

تم إصلاح الاستبدالات الخاطئة التي حوّلت دالة Python `max()` إلى `GREATEST()`.

الإصلاحات:
- صفحة تفاصيل الديون الخارجية.
- إدخال ساعات العمل الإضافي.
- إعادة دالة التجميع SQL الخاصة بتاريخ آخر فاتورة إلى `MAX(invoice_date)`.

تم الإبقاء على `GREATEST()` فقط داخل استعلامات PostgreSQL التي تحتاج دالة scalar.
